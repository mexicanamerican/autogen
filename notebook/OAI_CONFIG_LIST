# File: notebook/OAI_CONFIG_LIST

# Define the content of the file based on the requirements of the tests in test/agentchat/contrib

# Example content:
config_list = [
    {"model": "gpt-3.5-turbo", "api_type": "azure"},
    {"model": "gpt-4"}
]

# Ensure that the file has the appropriate permissions for access and readability by the tests
